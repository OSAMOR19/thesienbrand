# Optional. Leave unset to run entirely on the bundled mock data in
# src/data/products.ts — the app works fully without a backend.
#
# Set these to point the storefront at the schema + edge function in
# backend/supabase (see backend/README.md for setup).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
